aplicacao_individuos:
    llm_name: "gemini-1.5-flash"
    prompt: "Create a list of moons for each planet in the solar system. The result should be a raw JSON string with the planet names as keys and lists of moons as values. Don't include any markdown or HTML in the output."
